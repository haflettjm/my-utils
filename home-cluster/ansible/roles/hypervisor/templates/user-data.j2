#cloud-config
users:
  - name: fedora
    groups: wheel
    shell: /bin/bash
    sudo: ['ALL=(ALL) NOPASSWD:ALL']
    lock_passwd: true
    ssh_authorized_keys:
      - {{ ssh_public_key }}
ssh_pwauth: false
